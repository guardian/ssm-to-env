regions: [eu-west-1]
stacks: [deploy]
deployments:
  layer-deployment-test:
    type: aws-lambda-layer
    actions:
      - uploadLayer
      - updateLayer
    parameters:
      bucketSsmLookup: true
      fileName: ssm-to-env.zip
      layerName: ssm-to-env-layer-1664544948
